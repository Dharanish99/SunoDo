# Add project specific ProGuard rules here.
# Room, Compose, and Kotlin coroutines all ship their own consumer rules,
# so this stays empty until Stage 3 pulls in MediaPipe / ML Kit, which may need
# rules of their own (documented upstream in each library's release notes).
