<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Intentionally zero <uses-permission> entries.
      Reading the audio the user shares to us only needs the transient URI
      permission grant that comes attached to the incoming Intent itself —
      no storage permission, no accessibility service, no notification listener.
    -->

    <application
        android:name=".SunoDoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.SunoDo"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SunoDo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The entire integration point with WhatsApp/Telegram/any other app:
          standard Android Share Sheet target for audio, registered per the
          blueprint's ShareIntentReceiver module. No custom permissions,
          no per-app integration, no ToS surface with any third-party app.
        -->
        <activity
            android:name=".share.ShareReceiverActivity"
            android:exported="true"
            android:label="@string/share_target_label"
            android:theme="@style/Theme.SunoDo"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

    </application>

</manifest>
