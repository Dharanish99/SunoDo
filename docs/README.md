# docs/

`blueprint.md` is the original hackathon design document — problem
statement, system design, tech stack rationale, and business case. It's
the source of truth this whole prototype was built against; every stage's
commits and the two `README.md` files under `android/` reference specific
sections of it by number.
